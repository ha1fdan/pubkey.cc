# api.pubkey.cc serves JSON + WebSocket only, never HTML -- nothing should
# ever be loaded, executed, or framed from here.
add_header Content-Security-Policy "default-src 'none'; frame-ancestors 'none'" always;
